{% comment %} {%- liquid
  if cart == empty
    render 'icon-cart-empty'
  else
    render 'icon-cart'
  endif
-%}
<span class="visually-hidden">{{ 'templates.cart.cart' | t }}</span>
{%- if cart != empty -%}
  <div class="cart-count-bubble">
      {%- if cart.item_count < 100 -%}
        <span aria-hidden="true">{{ cart.item_count }}</span>
      {%- endif -%}
      <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
  </div>
{%- endif -%} {% endcomment %}


{% comment %} <a href="{{ routes.cart_url }}" class="cart-toggle" id="cart-icon-bubble-mobile">

  {% if cart == empty %}
    <div class="cart-text">Cart</div>
    <span class="cart-toggle--count-mobile" style="    text-align: center;">
      0
    </span>
  {% else %}
  <div class="cart-text">Cart</div>
  <span class="cart-toggle--count-mobile is-active" style="    text-align: center;">
    {%- if cart.item_count < 100 -%}
      <span aria-hidden="true">{{ cart.item_count }}</span>
    {% else %}
      {{ 'sections.header.cart_count' | t: count: cart.item_count }}
    {% endif %}
  </span>
  {% endif %}
</a> {% endcomment %}

<a href="{{ routes.cart_url }}" class="cart-toggle" id="cart-icon-bubble-mobile">
  {% if cart == empty %}
    <div class="cart-text">Cart</div>
    <span class="cart-toggle--count-mobile" style="    text-align: center;">
      0
    </span>
  {% else %}
  <div class="cart-text">Cart</div>
  <span class="cart-toggle--count-mobile is-active" style="    text-align: center;">
    {%- if cart.item_count < 100 -%}
      <span aria-hidden="true">{{ cart.item_count }}</span>
    {% else %}
      {{ 'sections.header.cart_count' | t: count: cart.item_count }}
    {% endif %}
  </span>
  {% endif %}
</a>